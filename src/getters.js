const workcenter = ['workcenter', 'workcenter']
const validWorkCenter = ['workcenter', 'validWorkCenter']
const needToChooseWorkCenter = ['workcenter', 'needToChooseWorkCenter']
const order = ['order', 'order']
const operations = ['order', 'operations']
const newConfirmations = ['confirmations', 'order', 'confirmations']
const sapConfirmations = ['confirmations', 'sapConfirmations']

export default { workcenter, validWorkCenter, needToChooseWorkCenter, order, operations, newConfirmations, sapConfirmations }
